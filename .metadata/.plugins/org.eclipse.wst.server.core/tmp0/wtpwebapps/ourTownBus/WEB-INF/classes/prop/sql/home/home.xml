<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ourTownBus.content.home.homeDAO">
	
	<select id="getBusNameGun" parameterType="hashmap" resultType="Integer">
		SELECT count(*)
		FROM t_busStopinfo
		WHERE arsId=#{arsId}
	</select>		
</mapper>